[project]
name = "bg3-audio-converter-autolabel"
version = "0.1.0"
description = "Pipeline to convert, decode, group, and label BG3 audio assets"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "alive-progress>=3.2.0",
    "python-dotenv>=1.2.2",
]
